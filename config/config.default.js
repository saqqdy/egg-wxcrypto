/**
 * egg-wxcrypto default config
 *
 * @member Config#wxcrypto
 * @property {String} SOME_KEY - some description
 */
exports.wxcrypto = {
	// token: '',
	// aesKey: '',
	// appId: '',
	options: {
		// normalizeTags: true,
		// buildXmlOptions: {}, // 透传用于生成 xml 字符串的配置
		// xmlOptions: {} // 透传用于解析 xml 字符串的配置
	}
}
